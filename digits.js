   function check(input){ 
       if(Number(input)){
           console.log('string contains only digits')
       }
           else{
               console.log('string does not contains only digit')
           }
   }
   check('123')
   check('gfg')